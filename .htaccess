<IfModule mod_rewrite.c>
    RewriteEngine On
    # Se o arquivo ou pasta solicitada não existir,
    # redireciona tudo para index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [QSA,L]
</IfModule>