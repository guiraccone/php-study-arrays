Options -Indexes

RewriteEngine On

# Redireciona todas as requisições que não correspondem a um arquivo ou diretório real
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Envia tudo para o index.php
RewriteRule ^ index.php [QSA,L]
